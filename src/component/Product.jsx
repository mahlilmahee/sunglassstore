import React from 'react';

const Product = () => {
    return (
        <div>
            <h3> Hello we are coming with product now get ready for it </h3>
        </div>
    );
};

export default Product;